<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="{{ asset('build/images/favicon.ico') }}" />
    <title>{% block title %}Amon Passage Secret{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body>
    <header id="header">
        <nav class="navbar">
            <ul class="navbar_left">
                <li class="actu">
                    <a class="hyperlink" href="{{path('actuality') }}">Actualités</a>
                </li>
                <li class="separator">
                    <img src="{{asset('build/images/vertical-line.png') }}" alt="">
                </li>
                <li class="design">
                    <a class="hyperlink" href="{{path('design_index') }}">Design métallique</a> 
                </li>
            </ul>
            <div class="logo-navbar">
                <a href="{{path('home') }}"><img src="{{ asset('build/images/logo-navbar.png') }}" alt="Le logo de la société Amon" width="100%" title="Aller à l'accueil"></a> 
            </div>
            <ul class="navbar_right">
                <li class="passages">
                    <a class="hyperlink" href="{{path('passage_index') }}">Passages secrets</a>
                </li>
                <li class="separator">
                    <img src="{{asset('build/images/vertical-line.png') }}" alt="">
                </li>
                <li class="contact">
                    <a class="hyperlink" href="{{asset('home/contact') }}">Contact</a>
                </li>
            </ul>
            {% if app.user %}
                <div class="mb-3">
                    Connecté en tant que : {{ app.user.username }}, <br><a href="{{ path('app_logout') }}">Se déconnecter</a>
                </div>
        {% endif %}
        </nav>
    </header>
    <div class="content">
        {% block body %}{% endblock %}
    </div>
    <footer class="footer">
            <p>©2021-2022 Amon-creation.fr - Tous droits réservés</p>
            <p><a href="{{path('home') }}">Mentions légales</a></p>
            <p class="login-button"><a href="{{path ('app_login') }}"><img src="{{ asset('build/images/padlock.png') }}" alt="Cadenas pour login"></a></p>
    </footer>

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>